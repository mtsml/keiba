CREATE TABLE race_backup (
    backup_id       INTEGER,
    race_id         BIGINT,
    race_date       DATE,
    weather         VARCHAR,
    distance        INTEGER,
    racecourse      VARCHAR,
    race_condition  VARCHAR,
    track_type      VARCHAR,
    track_condition VARCHAR,
    mawari          VARCHAR
);
